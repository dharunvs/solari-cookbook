# Controlled TypeScript example fixture

The executable TypeScript source uses the reviewed, aligned SDK spelling.

```typescript
import { Sandbox } from "@solarisdk/sandbox";

const sandbox = await Sandbox.create({ memMb: 2048 });
```
